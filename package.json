{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "defu": "^6.1.4",
    "nuxt": "^3.10.3"
  },
  "dependencies": {
    "@google/gemini-cli": "^0.1.7",
    "@pinia/nuxt": "^0.11.1",
    "pinia": "^3.0.3"
  }
}
